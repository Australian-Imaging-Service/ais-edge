# Secrets — never commit
config/management.env
config/edge-nodes.env
*.key
*.pem
*.token
secrets/

# Generated during install
kubeconfig-*
join-token-*
ais-edge-ca.crt
ca-bundle.crt

# OS files
.DS_Store
*.swp
*~

# Orthanc routing file
config/orthanc/routing.json

# Operator-edited per-site Orthanc deidentification profiles (copy from .template)
config/orthanc/deidentification-profile*.json
!config/orthanc/deidentification-profile*.json.template
